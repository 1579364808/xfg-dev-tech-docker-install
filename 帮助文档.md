# 支持 Ubuntu 24、centos 7.6\7.9 系统环境安装 

📢 如果执行过程中没有权限，可以在命令前加一个 `sudo` 如 `sudo yum install git`

## 1. 安装Git

**Centos**

```java
# sudo yum install git
```

**Ubuntu**

```java
# sudo apt update
# sudo apt install nodejs npm
# node -v
# npm -v
# apt-get install git
```

### 2. 下载安装脚本（github\gitcode）

**注意**

```java
cd /
mkdir dev-ops
cd dev-ops
```

之后在执行 git clone 操作，不要在 ~ 目录下，会有 root 权限。

```java
$ git clone https://gitcode.com/Yao__Shun__Yu/xfg-dev-tech-docker-install.git
$ git clone https://github.com/fuzhengwei/xfg-dev-tech-docker-install.git
```
### 3. 为所有脚本添加可执行权限

```java
$ find . -name "*.sh" -type f -exec chmod +x {} \;
```

### 4. 安装 Docker (会自动选择对应系统)

```java
$ ./run_install_docker.sh # 支持 Ubuntu/CentOS
```

docker 常用命令；

- docker --version          # 查看Docker版本
- docker ps                 # 查看运行中的容器
- docker images             # 查看本地镜像
- docker pull hello-world   # 拉取测试镜像
- docker run hello-world    # 运行测试容器
- docker rm nginx           # 删除服务
- docker rmi nginx          # 删除镜像
- docker exec -it nginx /bin/bash # 进入服务

### 5. 安装开发软件 (MySQL, Redis, Nacos 等)

```java
$ ./run_install_software.sh
```

### 6. 安装 JDK (可选，支持 8/17) - 查看帮助文档，Ubuntu 使用 apt 命令安装

```java
$ ./environment/jdk/install-java.sh -v 8
```

### 7. 安装 Maven - 查看帮助文档，Ubuntu 使用 apt 命令安装

```java
$ ./environment/maven/install-maven.sh
```

### 8. 安装 Terminal AI 工具(安装时候，推荐使用 opencode)

`terminal.sh` 是一个 **Terminal AI 助手安装脚本**，运行后会弹出交互式菜单，让你选择安装以下两种终端 AI 编程工具之一：

| 选项 | 工具 | 说明 |
|------|------|------|
| 1 | **Open Code (SST)** ⭐ 推荐 | 开源终端 AI 编程助手，内置免费模型，支持智谱AI GLM、小米AI等，国内可用 |
| 2 | **Claude Code (Anthropic)** | Anthropic 官方终端 AI 工具 |

脚本还会自动安装 `ripgrep (rg)`（OpenCode 所依赖的高速文本搜索工具），并将安装目录写入 Shell 配置文件（`.bashrc` / `.zshrc` 等），使命令全局可用。

```bash
./terminal.sh
```

- https://github.com/sst/opencode
- https://github.com/BurntSushi/ripgrep/releases/

>terminal.sh 脚本安装，已将 opencode、ripgrep 所需内容做好了镜像。让你可以便捷安装。

**安装完成后使用 Open Code：**

```bash
# 1. 进入你的项目目录（也可以新建一个）
cd /dev-ops

# 2. 启动 Open Code
opencode
```

Open Code 常用命令：

| 命令 | 说明 |
|------|------|
| `/help` | 查看帮助 |
| `/models` | 选择 AI 模型（免费模型、智谱AI GLM、小米AI 等） |
| `/clear` | 清空对话 |
| `/exit` | 退出 |
| `/login` | 登录账号 |
| `/logout` | 退出登录 |
| `/status` | 查看状态 |
| `/config` | 查看配置 |
| `/theme` | 切换主题 |

配置自定义全局模型（代理地址等）：

```bash
cd terminal
./opencode.sh
# 然后编辑配置文件（输入 i 进入编辑，编辑后按 Esc，输入 :wq 保存退出）
vim /root/.config/opencode/opencode.json
```

详细文档：https://opencode.ai/docs | 配置 MCP：https://opencode.ai/docs/mcp-servers/

---

关于如何使用 Docker 部署项目教程；[https://bugstack.cn/md/road-map/docker-deploy-project.html](https://bugstack.cn/md/road-map/docker-deploy-project.html)

